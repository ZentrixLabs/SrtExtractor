<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================ -->
    <!-- BASE BUTTON STYLE (Theme-Aware)             -->
    <!-- ============================================ -->
    
    <Style x:Key="BaseButtonStyle" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
        <Setter Property="Padding" Value="{StaticResource ThicknessM}"/>
        <Setter Property="Margin" Value="{StaticResource ThicknessXS}"/>
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <!-- Default Background/Foreground for theme-aware buttons -->
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusS}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Focusable="False"
                                        RecognizesAccessKey="True"
                                        TextElement.Foreground="{TemplateBinding Foreground}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="2" BlurRadius="4"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="Black" Opacity="0.2" ShadowDepth="1" BlurRadius="2"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================ -->
    <!-- TIER 1: PRIMARY BUTTONS (Main Actions)      -->
    <!-- Large, colored, icon + text                 -->
    <!-- Usage: Extract, Process Batch               -->
    <!-- ============================================ -->
    
    <!-- Primary Button - Large Size (Default Tier 1) -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightSemiBold}"/>
        <Setter Property="MinHeight" Value="48"/>
        <Setter Property="MinWidth" Value="180"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="20,12"/>
        <Setter Property="Tag" Value="{StaticResource PrimaryHoverBrush}"/>
    </Style>

    <!-- Primary Button - Large Size (Explicit) -->
    <Style x:Key="PrimaryButtonLarge" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="MinHeight" Value="52"/>
        <Setter Property="MinWidth" Value="220"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="24,14"/>
    </Style>

    <!-- Primary Button - Medium Size -->
    <Style x:Key="PrimaryButtonMedium" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="MinWidth" Value="140"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="16,10"/>
    </Style>

    <!-- ============================================ -->
    <!-- TIER 2: SECONDARY BUTTONS (Common Actions)  -->
    <!-- Medium, neutral, text only                  -->
    <!-- Usage: Probe, Pick File, Clear, Settings    -->
    <!-- ============================================ -->
    
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource SystemChromeMediumLowColor}"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource SystemBaseHighColor}"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource SystemBaseMediumLowColor}"/>
            </Setter.Value>
        </Setter>
        <Setter Property="MinHeight" Value="38"/>
        <Setter Property="MinWidth" Value="120"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="14,9"/>
    </Style>

    <!-- Secondary Button - Small Size -->
    <Style x:Key="SecondaryButtonSmall" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="12,7"/>
    </Style>

    <!-- ============================================ -->
    <!-- TIER 3: TERTIARY BUTTONS (Rare/Danger)     -->
    <!-- Small, outlined or red, minimal             -->
    <!-- Usage: Cancel, Remove, Delete               -->
    <!-- ============================================ -->
    
    <!-- Tertiary Button - Outlined Style -->
    <Style x:Key="TertiaryButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="MinWidth" Value="90"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="10,6"/>
    </Style>

    <!-- Danger Button (Destructive Actions) -->
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightSemiBold}"/>
        <Setter Property="MinHeight" Value="38"/>
        <Setter Property="MinWidth" Value="110"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="Tag">
            <Setter.Value>
                <SolidColorBrush Color="#B02E32"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Danger Button - Outlined (for less destructive actions) -->
    <Style x:Key="DangerButtonOutlined" TargetType="Button" BasedOn="{StaticResource TertiaryButton}">
        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================ -->
    <!-- LEGACY STYLES (Kept for compatibility)      -->
    <!-- Consider migrating to new hierarchy          -->
    <!-- ============================================ -->
    
    <!-- Success Button - Use PrimaryButton instead -->
    <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="Tag">
            <Setter.Value>
                <SolidColorBrush Color="#0E6A0E"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Warning Button - Use SecondaryButton or DangerButton instead -->
    <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
        <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightSemiBold}"/>
        <Setter Property="Tag">
            <Setter.Value>
                <SolidColorBrush Color="#E69500"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Accent Button - Use PrimaryButton instead -->
    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource InfoBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource InfoBrush}"/>
        <Setter Property="Tag" Value="{StaticResource PrimaryHoverBrush}"/>
    </Style>

    <!-- ============================================ -->
    <!-- ICON BUTTON (Small, Icon-Only)              -->
    <!-- ============================================ -->
    
    <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="MinWidth" Value="32"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontFamily" Value="{StaticResource IconFontFamily}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Tag" Value="{StaticResource BackgroundAltBrush}"/>
    </Style>

    <!-- ============================================ -->
    <!-- CLOSE BUTTON (X Button for Dialogs)        -->
    <!-- ============================================ -->
    
    <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
        <Setter Property="Content" Value="{StaticResource IconClose}"/>
        <Setter Property="ToolTip" Value="Close"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================ -->
    <!-- GHOST BUTTON (Transparent with Border)      -->
    <!-- ============================================ -->
    
    <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Tag">
            <Setter.Value>
                <SolidColorBrush Color="#F0F9FF"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

