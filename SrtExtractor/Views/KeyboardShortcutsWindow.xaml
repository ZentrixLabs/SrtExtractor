<Window x:Class="SrtExtractor.Views.KeyboardShortcutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Keyboard Shortcuts - SrtExtractor" 
        Height="600" Width="700"
        MinHeight="500" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="ShortcutKeyStyle">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="ShortcutDescStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="CategoryStyle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Margin" Value="0,16,0,8"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20,20,30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="⌨️ Keyboard Shortcuts" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Speed up your workflow with these keyboard shortcuts" 
                       FontSize="14" 
                       Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>
        
        <!-- Shortcuts List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel Margin="0,0,10,0">
                <!-- File Operations -->
                <TextBlock Text="📁 File Operations" Style="{StaticResource CategoryStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Open Video File" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Ctrl + O" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Switch to Batch Tab" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="Ctrl + B" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                </Grid>
                
                <!-- Extraction Operations -->
                <TextBlock Text="🎬 Extraction Operations" Style="{StaticResource CategoryStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Probe Tracks (Analyze Video)" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Ctrl + P" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Extract Selected Track to SRT" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="Ctrl + E" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Cancel Operation" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Ctrl + C  /  Esc" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                </Grid>
                
                <!-- Tools & Utilities -->
                <TextBlock Style="{StaticResource CategoryStyle}">
                    <Run Text="🔧 Tools &amp; Utilities"/>
                </TextBlock>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Re-detect External Tools" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="F5" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Show This Help" Style="{StaticResource ShortcutDescStyle}" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="F1" Style="{StaticResource ShortcutKeyStyle}" Margin="0,0,0,8"/>
                </Grid>
                
                <!-- Tips -->
                <Border Background="{StaticResource BackgroundAltBrush}"
                        BorderBrush="{StaticResource PrimaryBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="💡 Pro Tips" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Margin="0,0,0,8"/>
                        <TextBlock TextWrapping="Wrap" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="13"
                                   LineHeight="22">
                            <Run Text="• Hover over any button to see its keyboard shortcut in the tooltip"/><LineBreak/>
                            <Run Text="• Keyboard shortcuts work from anywhere in the application"/><LineBreak/>
                            <Run Text="• Use Escape key as a quick way to cancel any running operation"/><LineBreak/>
                            <Run Text="• Press Ctrl+B to quickly switch to the Batch tab for multi-file processing"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Close Button -->
        <Button Grid.Row="2" 
                Content="Close" 
                Click="Close_Click"
                Style="{StaticResource SecondaryButton}"
                HorizontalAlignment="Right"
                MinWidth="100"
                Margin="0,20,0,0"
                ToolTip="Close this help window"/>
    </Grid>
</Window>

