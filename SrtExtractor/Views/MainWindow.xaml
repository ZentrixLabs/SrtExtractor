    <Window x:Class="SrtExtractor.Views.MainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:SrtExtractor.Views"
            xmlns:converters="clr-namespace:SrtExtractor.Converters"
            mc:Ignorable="d"
            Title="SrtExtractor - MKV/MP4 Subtitle Extractor"
            Height="700" Width="1200"
            MinHeight="600" MinWidth="1000"
            Background="White">
    
    <Window.Resources>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:DoubleToVisibilityConverter x:Key="DoubleToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- DataGrid Selection Style -->
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF0078D4"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="{Binding State.QueueColumnWidth}" x:Name="QueueColumn" MinWidth="0"/>
        </Grid.ColumnDefinitions>
        
        <!-- Main Content Area -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

        <!-- File Selection Row -->
        <GroupBox Header="Video File" Grid.Row="0" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal">
                <Button Content="Pick Video..." 
                        Command="{Binding PickMkvCommand}"
                        IsEnabled="{Binding State.IsBusy, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                <TextBox Text="{Binding State.MkvPath}" 
                         IsReadOnly="True" 
                         Margin="4,0"
                         VerticalAlignment="Center"
                         MinWidth="300"/>
            </StackPanel>
        </GroupBox>

        <!-- Tool Status Panel -->
        <GroupBox Header="Tool Status" Grid.Row="1" Grid.Column="0">
            <StackPanel>
                <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- MKVToolNix Status -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,4">
                    <TextBlock Text="MKVToolNix:" FontWeight="Bold" Width="120"/>
                    <TextBlock Text="{Binding State.MkvToolNixStatus.IsInstalled, Converter={StaticResource BoolToStatusConverter}}" 
                               Margin="4,0"/>
                    <Button Content="Install via winget" 
                            Command="{Binding InstallMkvToolNixCommand}"
                            Visibility="{Binding State.MkvToolNixStatus.IsInstalled, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                            Background="LightGreen"/>
                    <Button Content="Browse..." 
                            Command="{Binding BrowseMkvToolNixCommand}"/>
                </StackPanel>

                <!-- Subtitle Edit Status -->
                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,4">
                    <TextBlock Text="Subtitle Edit CLI:" FontWeight="Bold" Width="120"/>
                    <TextBlock Text="{Binding State.SubtitleEditStatus.IsInstalled, Converter={StaticResource BoolToStatusConverter}}" 
                               Margin="4,0"/>
                    <TextBlock Text="(Auto-built)" 
                               FontStyle="Italic" 
                               Foreground="Gray" 
                               Margin="8,0"/>
                </StackPanel>

                <!-- FFmpeg Status -->
                <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,4">
                    <TextBlock Text="FFmpeg:" FontWeight="Bold" Width="120"/>
                    <TextBlock Text="{Binding State.FfmpegStatus.IsInstalled, Converter={StaticResource BoolToStatusConverter}}" 
                               Margin="4,0"/>
                    <TextBlock Text="(Auto-downloaded)" 
                               FontStyle="Italic" 
                               Foreground="Gray" 
                               Margin="8,0"/>
                </StackPanel>

                <!-- Tool Paths Display -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="20,4,0,4">
                    <TextBlock Text="{Binding State.MkvToolNixStatus.Path, StringFormat='Path: {0}'}" 
                               FontSize="10" 
                               Foreground="Gray"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="20,4,0,4">
                    <TextBlock Text="{Binding State.SubtitleEditStatus.Path, StringFormat='Path: {0}'}" 
                               FontSize="10" 
                               Foreground="Gray"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1" Margin="20,4,0,4">
                    <TextBlock Text="{Binding State.FfmpegStatus.Path, StringFormat='Path: {0}'}" 
                               FontSize="10" 
                               Foreground="Gray"
                               TextWrapping="Wrap"/>
                </StackPanel>
                </Grid>
                
                <!-- Re-detect Tools Button -->
                <Button Content="🔄 Re-detect Tools" 
                        Command="{Binding ReDetectToolsCommand}"
                        Background="LightBlue" 
                        Margin="0,8,0,0"
                        Padding="8,4"
                        HorizontalAlignment="Center"/>
                
                <!-- Correct SRT Button -->
                <Button Content="🔧 Correct SRT File" 
                        Command="{Binding CorrectSrtCommand}"
                        Background="LightGreen" 
                        Margin="0,4,0,0"
                        Padding="8,4"
                        HorizontalAlignment="Center"/>
                
                <!-- Cleanup Temp Files Button -->
                <Button Content="🧹 Cleanup Temp Files" 
                        Command="{Binding CleanupTempFilesCommand}"
                        Background="LightYellow" 
                        Margin="0,4,0,0"
                        Padding="8,4"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <!-- Settings Row -->
        <GroupBox Header="Settings" Grid.Row="1" Grid.Column="1">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <CheckBox Content="🎬 Enable Batch Mode (Process Multiple Files)" 
                              IsChecked="{Binding State.IsBatchMode}"
                              VerticalAlignment="Center"
                              FontWeight="Bold"
                              Foreground="DarkBlue"
                              Margin="0,0,20,0"/>
                    <RadioButton Content="Prefer forced subtitles" 
                                 IsChecked="{Binding State.PreferForced}"
                                 GroupName="SubtitlePreference"
                                 VerticalAlignment="Center"
                                 Margin="0,0,20,0"/>
                    <RadioButton Content="Prefer CC (Closed Captions)" 
                                 IsChecked="{Binding State.PreferClosedCaptions}"
                                 GroupName="SubtitlePreference"
                                 VerticalAlignment="Center"
                                 Margin="0,0,20,0"/>
                </StackPanel>
                
                <!-- Batch Mode Instructions -->
                <Border Background="#E3F2FD" 
                        BorderBrush="#2196F3" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="12" 
                        Margin="0,10,0,0"
                        Visibility="{Binding State.IsBatchMode, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="📋 How to use Batch Mode:" 
                                   FontWeight="SemiBold" 
                                   FontSize="13" 
                                   Foreground="#1976D2"
                                   Margin="0,0,0,6"/>
                        <TextBlock Text="1. Enable Batch Mode above ✓" 
                                   FontSize="12" 
                                   Foreground="#1976D2"
                                   Margin="0,2,0,0"/>
                        <TextBlock Text="2. Drag &amp; drop MKV/MP4 files anywhere on this window" 
                                   FontSize="12" 
                                   Foreground="#1976D2"
                                   Margin="0,2,0,0"/>
                        <TextBlock Text="3. Click 'Process Batch' to extract subtitles from all files" 
                                   FontSize="12" 
                                   Foreground="#1976D2"
                                   Margin="0,2,0,0"/>
                        <TextBlock FontSize="11" 
                                   FontStyle="Italic" 
                                   Foreground="#666666"
                                   Margin="0,6,0,0"
                                   TextWrapping="Wrap">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="💡 All files will use your preferred settings: {0} subtitles, {1} language">
                                    <Binding Path="State.PreferForced" Converter="{StaticResource BoolToStatusConverter}"/>
                                    <Binding Path="State.OcrLanguage"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Border>
                
                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <TextBlock Text="OCR Language:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <ComboBox SelectedItem="{Binding State.OcrLanguage}" 
                              ItemsSource="{Binding State.AvailableLanguages}"
                              Width="80"
                              Margin="0,0,20,0"/>
                    <TextBlock Text="File Pattern:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBox Text="{Binding State.FileNamePattern}" 
                             Width="200"
                             ToolTip="Pattern: {basename}.{lang}{forced}{cc}.srt"/>
                </StackPanel>

                <!-- Network Warning -->
                <Border Background="LightYellow" 
                        BorderBrush="Orange" 
                        BorderThickness="1" 
                        CornerRadius="4" 
                        Padding="8" 
                        Margin="0,10,0,0"
                        Visibility="{Binding State.ShowNetworkWarning, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="⚠️ Network File Detected" 
                                   FontWeight="Bold" 
                                   Foreground="DarkOrange"/>
                        <TextBlock Text="{Binding State.NetworkWarningMessage}" 
                                   TextWrapping="Wrap" 
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <GroupBox Header="Actions" Grid.Row="2" Grid.Column="0">
            <StackPanel>
                <!-- Single File Mode Actions -->
                <StackPanel Orientation="Horizontal" 
                            Visibility="{Binding State.ShowSingleFileMode, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Button Content="Probe Tracks" 
                            Command="{Binding ProbeCommand}"
                            IsEnabled="{Binding State.CanProbe}"
                            Background="LightBlue"/>
                    <Button Content="Extract Selected → SRT" 
                            Command="{Binding ExtractCommand}"
                            IsEnabled="{Binding State.CanExtract}"
                            Background="LightCoral"/>
                    <Button Content="Cancel" 
                            Command="{Binding CancelCommand}"
                            IsEnabled="{Binding State.IsProcessing}"
                            Background="Orange"
                            Visibility="{Binding State.IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
                
                <!-- Batch Mode Actions -->
                <StackPanel Orientation="Horizontal" 
                            Visibility="{Binding State.ShowBatchMode, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Button Content="🚀 Process Batch" 
                            Command="{Binding ProcessBatchCommand}"
                            IsEnabled="{Binding State.HasBatchQueue}"
                            Background="#4CAF50"
                            Foreground="White"
                            FontWeight="Bold"
                            FontSize="14"
                            Padding="16,8"
                            Margin="0,0,10,0"/>
                    <Button Content="Cancel Batch" 
                            Command="{Binding CancelCommand}"
                            IsEnabled="{Binding State.IsProcessing}"
                            Background="#FF9800"
                            Foreground="White"
                            FontWeight="Bold"
                            Padding="16,8"
                            Visibility="{Binding State.IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
                
                <!-- Common Actions -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Clear Log" 
                            Click="ClearLog_Click"
                            Background="LightGray"/>
                    <Button Content="About" 
                            Click="About_Click"
                            Background="LightGreen"
                            Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Processing Indicator -->
        <GroupBox Header="Processing" Grid.Row="2" Grid.Column="1" Visibility="{Binding State.IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                            Width="20" 
                            Height="20" 
                            Margin="0,0,10,0"/>
                <TextBlock Text="{Binding State.ProcessingMessage}" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <!-- Track List -->
        <GroupBox Header="Subtitle Tracks" Grid.Row="3" Grid.ColumnSpan="2"
                  Visibility="{Binding State.ShowSingleFileMode, Converter={StaticResource BoolToVisibilityConverter}}">
            <DataGrid ItemsSource="{Binding State.Tracks}" 
                      SelectedItem="{Binding State.SelectedTrack}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" 
                                        Binding="{Binding Id}" 
                                        Width="40"/>
                    <DataGridTextColumn Header="Codec" 
                                        Binding="{Binding Codec}" 
                                        Width="120"/>
                    <DataGridTextColumn Header="Language" 
                                        Binding="{Binding Language}" 
                                        Width="80"/>
                    <DataGridTextColumn Header="Type" 
                                        Binding="{Binding TrackType}" 
                                        Width="80"/>
                    <DataGridCheckBoxColumn Header="Forced" 
                                            Binding="{Binding Forced}" 
                                            Width="60"/>
                    <DataGridTextColumn Header="Recommended" 
                                        Binding="{Binding IsRecommended}" 
                                        Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="⭐ Recommended"/>
                                <Setter Property="Foreground" Value="Green"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRecommended}" Value="False">
                                        <Setter Property="Text" Value=""/>
                                        <Setter Property="Foreground" Value="Transparent"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Bitrate" 
                                        Binding="{Binding Bitrate, StringFormat='{}{0:N0} bps'}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Frames" 
                                        Binding="{Binding FrameCount}" 
                                        Width="70"/>
                    <DataGridTextColumn Header="Duration" 
                                        Binding="{Binding Duration, Converter={StaticResource DurationConverter}}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Name" 
                                        Binding="{Binding Name}" 
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Log Display -->
        <GroupBox Header="Log" Grid.Row="4" Grid.ColumnSpan="2">
            <ScrollViewer Height="150" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox Text="{Binding State.LogText}" 
                         IsReadOnly="True" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         Background="Black" 
                         Foreground="LightGreen"
                         FontFamily="Consolas"
                         FontSize="11"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Drag and Drop Overlay - Must be last to appear on top -->
        <Border x:Name="DragOverlay" 
                Background="#E3F2FD" 
                BorderBrush="#2196F3" 
                BorderThickness="2" 
                CornerRadius="8"
                Opacity="0.95"
                Visibility="Collapsed"
                Margin="20"
                Grid.RowSpan="7">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                <TextBlock Text="📁" 
                           FontSize="48" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <TextBlock Text="Drop video files here" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Foreground="#1976D2"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="to add to batch queue" 
                           FontSize="16" 
                           FontWeight="Normal" 
                           Foreground="#1976D2"
                           HorizontalAlignment="Center"
                           Margin="0,2,0,0"/>
                <TextBlock Text="Supports: MKV, MP4" 
                           FontSize="12" 
                           Foreground="#666666"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
        </Grid>

        <!-- Queue Panel -->
        <GroupBox Header="🎬 Batch Queue" Grid.Column="1" 
                  Visibility="{Binding State.QueueColumnWidth, Converter={StaticResource DoubleToVisibilityConverter}}"
                  Background="#F8FAFC"
                  BorderBrush="#E2E8F0"
                  BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Queue Instructions -->
                <Border Grid.Row="0" Background="#F3F4F6" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="📁 Drag &amp; drop video files here" FontWeight="SemiBold" FontSize="13" Foreground="#374151" HorizontalAlignment="Center"/>
                        <TextBlock Text="Supports: MKV, MP4" FontSize="11" Foreground="#6B7280" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- Queue Header -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Files in Queue:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding State.BatchQueue.Count, StringFormat='{}{0}'}" 
                               FontWeight="Bold" 
                               Foreground="Blue"
                               VerticalAlignment="Center"
                               Margin="4,0,0,0"/>
                    <Button Content="Clear All" 
                            Command="{Binding ClearBatchQueueCommand}"
                            Background="LightCoral"
                            Margin="10,0,0,0"
                            Padding="4,2"
                            FontSize="10"
                            Visibility="{Binding State.HasBatchQueue, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
                
                <!-- Queue List -->
                <Grid Grid.Row="2">
                    <ListBox ItemsSource="{Binding State.BatchQueue}"
                             Background="White"
                             BorderBrush="Gray"
                             BorderThickness="1"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Margin="2" Padding="8">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                                   Text="{Binding FileName}" 
                                                   FontWeight="Bold" 
                                                   TextWrapping="Wrap"
                                                   MaxWidth="200"/>
                                        <TextBlock Grid.Row="1" Grid.Column="0" 
                                                   Text="{Binding FormattedFileSize}" 
                                                   FontSize="10" 
                                                   Foreground="Gray"
                                                   Margin="0,2,0,0"/>
                                        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,2,0,0">
                                            <TextBlock Text="{Binding NetworkIndicator}" FontSize="12"/>
                                            <TextBlock Text="{Binding FormattedEstimatedTime}" FontSize="10" Foreground="Blue" Margin="4,0,0,0"/>
                                        </StackPanel>
                                        
                                        <Button Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" 
                                                Content="×" 
                                                Command="{Binding DataContext.RemoveFromBatchCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Background="LightCoral"
                                                Foreground="White"
                                                FontWeight="Bold"
                                                Width="20" Height="20"
                                                Margin="4,0,0,0"
                                                Padding="0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <!-- Empty Queue Placeholder -->
                    <Border Background="#F9FAFB" 
                            BorderBrush="#E5E7EB" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="20"
                            Visibility="{Binding State.HasBatchQueue, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="📂" 
                                       FontSize="32" 
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="No files in queue" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="#374151"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Drag &amp; drop video files here to add them to the queue" 
                                       FontSize="12" 
                                       FontStyle="Italic" 
                                       Foreground="#6B7280" 
                                       HorizontalAlignment="Center"
                                       Margin="0,4,0,0"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>
                
                <!-- Queue Progress -->
                <StackPanel Grid.Row="3" Margin="0,8,0,0" 
                            Visibility="{Binding State.HasBatchQueue, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding State.BatchProgressText}" 
                               FontWeight="Bold" 
                               HorizontalAlignment="Center"
                               FontSize="11"/>
                    <ProgressBar Value="{Binding State.BatchProgressPercentage}" 
                                 Height="15" 
                                 Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding State.BatchProgressMessage}" 
                               HorizontalAlignment="Center" 
                               FontStyle="Italic" 
                               FontSize="10"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
